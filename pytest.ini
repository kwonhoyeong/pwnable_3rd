[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: End-to-end smoke tests
    unit: Unit tests for individual services
    integration: Integration tests with external APIs (requires API keys)
